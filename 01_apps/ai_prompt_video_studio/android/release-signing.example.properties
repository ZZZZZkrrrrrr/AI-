# Copy this file to android/release-signing.properties on a secure build machine.
# Never commit android/release-signing.properties or real keystore files.

storeFile=REPLACE_WITH_ABSOLUTE_OR_SECURE_CI_KEYSTORE_PATH
storePassword=REPLACE_WITH_PASSWORD_MANAGER_OR_CI_SECRET
keyAlias=REPLACE_WITH_UPLOAD_KEY_ALIAS
keyPassword=REPLACE_WITH_PASSWORD_MANAGER_OR_CI_SECRET

# Recommended first Google Play release path:
# 1. Build an Android App Bundle (.aab).
# 2. Enroll in Play App Signing.
# 3. Use this keystore as the upload key, not as a committed app signing key.
